T-S0.1 自报：已在 task/t-s0.1-backend-skeleton 分支完成治理基线（AGENTS.md、.gitignore 增补 .reasonix(-runs)/）+ FastAPI 后端骨架（pyproject.toml；psyapp 包 main/config/logging_conf/response/db/models/enums；tests 4 用例）。跑过：`uv pip install -p .venv -e "app/backend[dev]"` 成功（18 包）、`import psyapp.main` PASS、`pytest app/backend/tests -q` 4 passed、`ruff check app/backend` 零错误、uvicorn 8660 启动 + `/health` 200 `{"status":"ok",...}` + 404 统一 `{ok:false}`、7 表建齐且 sessions/segments/records/clients 带 user_id、dev 用户已播种、全量 grep 无 sk- 明文。自报不作数，请编排方实测验收。
