# Task T-S1.7: OMNI_TRANSCRIBE_PROMPT 升 v2——称呼语铁证锚定（修复角色翻转缺陷）

## 背景与契约

陛下拍板（`docs/feedback.md` FB-009 原话）：「把增强版 prompt 升为线上 v2」。

**大统领先导实证（2026-08-28，05号 8分05秒对抗音频，已验证，勿重新实验）**：
- 现线上 prompt 直转 05 号：32 轮，逐轮对齐黄金稿（`tests/audio/05_xhs_good.md`，28轮）角色正确率仅 **24%**（6对/19错）——「角色反转」对话（来访者强势追问咨询师私生活）触发模型「提问者=咨询师」刻板先验，全篇翻转，生产环境会话41 已复现。
- 增强版 prompt（下方原文）：角色正确率 **81%**（21对/5错），**两次复跑稳定**，耗时不变（8.2s→9.1s），并成功保留「（笑）」非语言信息。
- 剩余 5 处错误集中在黄金稿 #13/#17-20 内容歧义区，ASR 管线（paraformer+deepseek）交叉验证同样错在该区——非 prompt 可解，不在本卡范围。

分支：`task/t-s1.7-omni-prompt-v2`（从 main 开）。提交消息：`[T-S1.7] type: 描述`。
**你不 merge、不 push、不打 tag**；自报写 `docs/handoffs/task-T-S1.7-self-report.md`。

## 钉死的改动（极小范围，只改一处常量）

`app/backend/src/psyapp/providers/omni.py`：

1. `OMNI_TRANSCRIBE_PROMPT` 整段替换为以下 v2 原文（**照抄，一个字不要改**；同时把常量上方注释从「大统领先导探针验证过的转写+清洗 prompt（FB-008 拍板，照抄勿改写）」改为「转写+清洗 prompt v2（FB-009 陛下拍板升级，称呼语铁证锚定修复角色翻转；照抄勿改写）」）：

```
请完整转写这段心理咨询录音，并直接输出清理后的对话稿。要求：
1. 先听完全篇判断说话人角色（咨询师/来访者），在每一轮标注角色。角色判定铁律：
   a) 称呼语优先：若一方称呼另一方为「某某老师」，说话人一定不是该老师本人，对方才是；
   b) 不要假设「提问者=咨询师」：存在来访者强势追问、咨询师被动防御的对话，角色判定以称呼语和自述内容为准；
   c) 全篇一致性：同一人的声音在全篇中角色标注必须一致，若发现中途翻转，重新核对全篇。
2. 按对话轮次组织输出：同一人连续说的话合并为一轮；纯语气词（嗯/啊/对）不单独成轮，并入相邻轮次或删除。
3. 清理口语：去除填充词，合并被拆散的句子，修正明显的语音识别错误和指代（根据上下文判断他/她），修正重复字（如「是是」→「是」）。
4. 保留原意和说话风格，不添加、不总结；笑声、叹气等非语言信息用（笑）等形式保留。
5. 输出格式（每轮一行）：轮次号\t角色\t内容
```

注意：第 5 条中的 `\t` 是**真实制表符**（与 v1 现状一致，v1 源码里就是真实制表符），不是反斜杠字面量。

2. **其他一切不动**：`parse_omni_transcript()` 解析逻辑、请求体结构（modalities/enable_thinking=false）、services/routes/前端、asr 路径——全部不碰。输出格式（轮次号\t角色\t内容）与 v1 相同，解析无需变化。

## 测试

- 现有 54 项测试全过（回归，含 test_omni_mode.py 的请求结构/解析/重试用例）。
- 若 test_omni_mode.py 中有对 OMNI_TRANSCRIBE_PROMPT 内容的断言（如「照抄探针」子串检查），随本卡同步更新断言为 v2 对应内容；无则不加新测试。
- 禁止真实调 API 的单测（沿用现有 fake provider 约定）。

## 禁止清单

- 不改 asr 路径任何行为；不动 `.env`；不引新依赖；不新建测试音频/黄金稿文件。
- 不改解析逻辑、不加配置开关、不做「prompt 版本化配置」之类镀金——本卡只换常量。
- 不把音频 base64 写日志；不 merge、不 push、不打 tag。

## 验收标准（大统领逐条实测）

1. pytest 全过 + ruff + 导入自检 + 前端构建。
2. **05号对抗音频实测**（大统领）：走线上 API omni 模式上传 `tests/audio/05_xhs_audio.mp3`，角色标注对照黄金稿 ≥80% 正确（基线 24%）。
3. **回归**（大统领）：01 号合成音频 omni 模式正常出稿（确认常规音频未被 v2 规则带偏）。
4. 自报在 `docs/handoffs/task-T-S1.7-self-report.md`。

## 门禁

构建 → 测试 → 静态；提交前 `git diff` 自查只含本卡范围（omni.py 常量 + 可能的测试断言同步）。
