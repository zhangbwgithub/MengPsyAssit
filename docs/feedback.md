# 反馈台账（陛下的修改意见记录）

> 陛下每次反馈逐条记录在此：原文要点 → 处理结论 → 落实去向。由大统领维护，只增不删。

---

## FB-001 · 2026-08-27 · 角色分离流程重构 + 记录风格

### 反馈 1：转写阶段先不定义角色，仅做角色分离，用代号区分（男A/女A/男B…），去掉说话人映射选择框

**结论：✅ 采纳，纳入任务卡 T-S1.1**

- 落实：转写阶段 segments 只存说话人代号（按首次出现顺序 A/B/C…），前端去掉 `speaker_zero` 映射单选，POST /sessions 移除该参数。
- 一处技术偏差说明：paraformer-v2 说话人分离**不返回性别信息**，无法直接给出「男A/女A」式代号。落地为性别中性代号「说话人 A / 说话人 B / …」（如后续接入带性别识别的 ASR 或 LLM 从内容推断出性别，可再升级为 男A/女A 式标签）。

### 反馈 2：清理阶段由 LLM 通过对话内容判定具体角色（咨询师/来访者，同角色多人用 ABC 区分），该阶段显示做成对话气泡、不同人不同颜色

**结论：✅ 采纳，纳入任务卡 T-S1.1**

- 落实：clean 阶段升级为「口语清理 + 角色判定」，LLM 输出结构化 JSON（角色归属 + 逐段清理文本）；segments 表增加 role/role_label/cleaned_content 列。
- 前端：清理后文本从纯段落改为对话气泡渲染；气泡按说话人代号着色（每人一色），咨询师靠左/来访者靠右，标签显示判定后的角色（同角色多人显示 咨询师A/来访者B 式标签）。

### 反馈 3：咨询记录格式要符合精神动力学咨询师的叙述风格；先在 GitHub（deepwiki）找现成 plugin/skill 嵌入，找不到就算了

**结论：🔍 已调研，暂无可用现成件，按陛下指示暂缓（未来经 plugin/skill 机制接入）**

- 调研范围（2026-08-27，大统领实测）：
  - GitHub 仓库搜索：`psychodynamic skill` / `psychodynamic formulation` / `therapy-notes` / `psychotherapy record` / `精神动力学` 等多组关键词
  - anthropics/skills 官方技能仓库、davila7/claude-code-templates（120+ scientific skills，含 clinical-reports/treatment-plans，均为西医临床文书向，无精神动力学叙述风格）
  - GitHub topic 页（ifs-therapy、claude-code-skills 等）
- 最接近的候选（均不匹配，记录备查）：
  | 候选 | 内容 | 不匹配原因 |
  |---|---|---|
  | `luof9924-ones/house-tree-person-psychodynamic-skill` | 房树人绘画的精神动力学解读技能 | 面向绘画投射测验，非咨询记录 |
  | `sethmblack/skill-organizational-psychodynamics-analysis` | 组织心理动力学分析 | 面向组织管理，非临床咨询 |
  | `alirezarezvani/claude-code-skills-factory`（Health SDK Builder） | 德语 PTV 10 医保申请生成，含 psychodynamic formulation | 面向德国医保申报文书，德语、格式固定，非中文咨询记录叙述 |
- 处理：记录生成 prompt 暂维持现有客观记录风格（v1）；待未来陛下的精神动力学风格 plugin/skill 就绪后，经 prompts 模板机制接入（record 模板版本化已支持）。

---

## FB-002 · 2026-08-27 · 气泡布局回退 + 清理/记录阶段太慢

### 反馈 1：转写对话稿气泡都居中了，要恢复原来的布局

**结论：✅ 采纳，纳入任务卡 T-S1.2**

- 根因：陛下上传的真实音频（会话19）清理阶段失败 → 角色未判定 → 前端兜底走「未判定=居中」，全部气泡居中。
- 落实：① 未判定/失败态气泡恢复左右布局（按代号首现序 A/C/…靠左、B/D/…靠右，与原版 T 左 P 右视觉一致）；② 修复清理失败根因（纯语气词段清理成空串导致校验失败+无效重试）。

### 反馈 2：清理阶段和记录阶段耗时太久

**结论：✅ 采纳——陛下指示不做 provider 测速；慢的根因与现状如实记录，provider 维持 MIMO 不动**

- 实测现象（jobs 日志）：59 段真实音频 clean 单次 ~150s、失败重试两轮共 ~5 分钟；record ~100s。MIMO 长 JSON 输出是主要耗时。
- 处理（不测速）：① provider 维持 MIMO（陛下既定决策），若未来想换，`.env` 一行 `LLM_PROVIDER=qwen/deepseek` 即可，无需测试；② 给 jobs 表加时间戳（当前无，排障靠猜日志）；③ 减少无效重试（失败根因修复后自然省时）。

---

## FB-003 · 2026-08-27 · 转写显示简化 + 语义重拼/纠错 + 清洗 skill sourcing + 进度条

> 陛下已确认理解：转写阶段气泡居中是角色未判定的中间态，清理完成后回归左右对话布局。

### 反馈 1：转写阶段的文字和角色可以简化显示

**结论：✅ 采纳，纳入任务卡（T-S1.3 规划）**

- 落实：转写完成但清理未完成时，气泡展示简化（去掉冗余元信息，紧凑样式），明确这是「原始转写」中间态。

### 反馈 2：断句不准（最大问题）——清理阶段按语义重新拼接划分角色 + 语义纠错（如 他→她）

**结论：✅ 采纳，纳入任务卡（T-S1.3 规划，本条为核心）**

- 实测问题：01:03/01:07 同一人一句话被拆成上下两半；02:45/02:49/02:51 同一人连续三段口语碎片——paraformer 按语音停顿断句，不懂语义。
- 落实方向：清理阶段升级为「语义重拼」——LLM 按语义合并同一说话人的碎片段、重划段落边界，同时纠正指代（如上下文是来访者谈妻子，后文「他」纠为「她」）与明显错别字。需要新的输出结构（合并后段落与原始段不再 1:1）。

### 反馈 3：sourcing 对话转写清洗 skill（断句/语法/别字），非常重要

**结论：✅ 调研完成，找到高匹配方法论——采用「蒸馏嵌入」而非整体引入（详见下）**

- 重点考察：断句修正 / 语法纠正 / 别字修正 三类能力。
- 调研范围（2026-08-27，大统领实测）：GitHub 仓库搜索 14 组关键词（asr correction/transcript cleanup/speech-to-text post-processing/diarization merge 等）+ awesome-agent-skills 目录 + 各候选仓库源码精读。
- **最优匹配：`zxkane/audio-transcriber` v1.7.1**（★8，FunASR/Paraformer 家族 + LLM 后处理的成熟 agent skill），其方法论与陛下诉求逐条对上：
  | 其方法 | 对应陛下诉求 |
  |---|---|
  | Phase 2 规则合并：同一说话人连续段（间隔<2s）先合并再交给 LLM | 断句碎片（02:45/02:49/02:51）预合并 |
  | Phase 3 LLM 清理规则「合并口吃/重复表达为流畅句子」 | 语义重拼 |
  | 「基于上下文修正明显 ASR 错误（同音字）」 | 他→她 这类指代/别字纠正 |
  | 「修正语法错误，保持可读性」「保留原意不新增内容」 | 语法纠正且不镀金 |
  | 长音频按 15 分钟分块调用（`chunk_by_duration(900000ms)`） | 顺带缓解长转写的清理耗时 |
- 次相关候选（均不采用，记录备查）：`danielrosehill/Voice-Cleanup-Prompt-Experiment`（10 档清理力度对照实验，结论：Moderate 档=去填充词+STT 纠错是转写清理甜点，重度改写会失真）、`elbruno/ElBruno.S1Mini`（本地填充词清理）、`yunye123/interview-soul-editor`（访谈逐字稿剪辑向）。
- **处理决策：不整体引入该 skill**（它是完整本地转写管线，与我们的云端 paraformer 主链路重叠且依赖重），而是把其 Phase 2+3 方法论**蒸馏进 clean prompt v3** + 后端规则预合并 + 分块机制——在 T-S1.3 任务卡中落实。调研来源：https://github.com/zxkane/audio-transcriber

### 反馈 4：上方增加进度条，让用户清楚目前在哪个阶段

**结论：✅ 采纳，纳入任务卡（T-S1.3 规划）**

- 落实：上传区上方三阶段进度条（转写 → 清理+角色判定 → 记录生成），随会话状态推进高亮；失败态显示失败所在阶段。

---

## FB-004 · 2026-08-27 · 「处理失败」排查——长转写 seq 数错

### 反馈：为什么会显示「处理失败」？

**结论：✅ 根因查明，修复卡 T-S1.4 已派单**

- 现场：陛下上传 56 段真实音频（会话25），清理阶段失败，错误 `source_seqs 未覆盖全部输入段落（期望[0..55]，实际[0..56]）`。
- 根因：T-S1.3 的 clean v3 输入只给 `代号: 文本` 逐行稿，让 LLM 自己数行号当 seq——56 段长文本下模型多数出一个虚构序号，两次尝试均被严格校验拒绝。
- 修复：输入每行显式带 `[seq]` 编号（含分块路径的全局口径），prompt 明确「逐字引用编号、不得数行号」；严格校验保留作安全网。
- 验收：修复后重跑会话25 同音频必须 done。

---

## FB-005 · 2026-08-28 · cermethod.md 推荐组合调研 + Hermes skill + DeepSeek-v4-flash 清洗模型

### 反馈：调研 cermethod.md 推荐组合是否是好方案；是则做成 Hermes skill；同时启用 DeepSeek-v4-flash 作为清洗模型

**结论：✅ 部分采纳——框架整体不适合直连（离线评测 harness），但其核心方法论极有价值，已蒸馏封装成 skill；清洗模型切换已派单（T-S1.5）**

**调研实证（2026-08-28，大统领）：**

1. **teamtee/LLM-ASR-Error-Correction（★19，华科 Dain 团队）**：精读源码+配置+PromptList 后判定——它是**离线批量评测框架**：需要 gold label（text 文件 + 标签文件）、计算 CER 改进，1758 文件中绝大多数是 AISHELL/Librispeech 实验结果。**无法直接接入我们的在线管线**（我们无 gold label、是 live pipeline）。但其方法论是金子：
   - **拼音推理链纠错**：定位错字 → 转有声调拼音 → 生成同音候选 → 按上下文选择——专治 ASR 同音字错误（本项目「他/她」类错误的根源）
   - **保守阈值机制**：读音差别过大/没把握时**保留原文**，用 `<改>/<原>` 标记——防过度改写，与 CoC 阈值思想同源
   - **实测参数**：temp=0.2 / top_p=0.4、每请求 30 句批量
   - 实测效果：AISHELL-1 DeepSeek CER -13%（5.17→4.48）
2. **CoC 阈值思想**：已体现在上述「保守纠错」原则中；两遍清洗管线（TranscriptEnhancer）与本项目单次语义重拼相比性价比低（多一次全量 LLM 调用=耗时翻倍），不采纳。
3. **ct-punc 标点恢复**：不适用——本项目用云端 paraformer-v2 录音文件识别（自带标点），ct-punc 是本地 FunASR 模型。
4. **pycorrector**：独立中文纠错库，需额外依赖与模型下载，其能力已被 LLM 清洗覆盖，不采纳。
5. **关键新发现——deepseek-v4-flash 是推理模型**（大统领实测探针）：
   - 默认带 thinking：简单纠错请求输出 245 tokens 中 240 个是推理 tokens，3.0s
   - `thinking: {type: disabled}`：**0.7s**（4.3x 提速），输出质量不变
   - DeepSeek API 硬性要求：关闭思考时 temperature 必须 = 0.2
   - `.env` 已有 DEEPSEEK_API_KEY，provider 工厂早已接好（默认模型正是 deepseek-v4-flash）

**处理：**
- ✅ 封装为 Hermes skill `asr-transcript-cleanup`（teamtee 拼音推理链 + CoC 保守阈值 + zxkane 管线方法论 + 模型接入清单），可复用于任何 ASR 后处理项目
- ✅ 工作流改进派单 T-S1.5：清洗阶段独立切换到 deepseek-v4-flash（关 thinking、temp=0.2），record 阶段维持 MIMO 不动（陛下 T-S0.6 既定决策）；clean prompt 升 v4 吸收拼音推理链方法论
- ⏳ 验收标准：陛下真实音频实测耗时对比（基线：59 段 124.8s / 56 段 293.8s，MIMO v3）

---

## FB-006 · 2026-08-28 · record 阶段也试一下 deepseek-v4-flash

### 反馈：陛下追加口头指令（T-S1.5 验收期间）

**结论：✅ 采纳，追加卡 T-S1.5b（同分支增量），record 阶段也切 deepseek-v4-flash**

- T-S1.5 原定 record 维持 MIMO（T-S0.6 决策），陛下现明确放开：record 也可试新模型。
- 落实：`record_llm_provider` 独立配置，默认 deepseek；record prompt（v2）不动；验收同 T-S1.5（真实音频重跑 + 耗时对比）。实测：clean 294s→8s、record 48s→3s，全链 352s→21s，质量不降。

---

## FB-007 · 2026-08-28 · Kimi 音频直转对照实验——技术原理 + 校对 + 调教清洗 skill

### 反馈：陛下上传同一段 MP3 到 Kimi chat 获得高质量转写清洗结果（goodresult.md），要求：1) 解释技术原理；2) 用其校对现有输出、总结差异、调教清洗 skill

**结论：✅ 完成——校对报告 `/home/houmo/meng/proofread_ts30_vs_kimi.md`，skill `asr-transcript-cleanup` 已按四条调教结论升级**

- 技术原理：Kimi chat 走音频多模态链路（音频→编码器→token，模型直接"听"音色/韵律/轮换做分离，转写+清洗+角色一步完成，全篇上下文消歧）；我方管线是「paraformer 文本分离 → 纯文本清洗」，声学信息在 ASR 边界丢光。注：chat 音频链路并非 K3 API 本尊（K3 API 只收文本/图像/视频）。
- 校对结果：19 轮中 11 一致 / 4 偏差 / 4 明显错误（说话人错标 3 处、听错未纠 2 处、幽灵轮次 5 处）。
- 差异四根因：①输入模态（听 vs 读）②paraformer 三种场景必碎（句中被切/语气词翻转/轻声尾音）③清洗目标（保段 vs 保轮次）④规则禁止跨说话人修复。
- skill 调教四条已落实：轮次化输出、跨说话人碎片修复升为要求、听错重建加强、记录多模态直转路线与代价。
- 待拍板：clean prompt 升 v5 把同样规则落到管线（本报告即调教语料）。

---

## FB-008 · 2026-08-28 · Qwen3.5-Omni 探针大捷 → 双模式管线（陛下拍板）

### 反馈：陛下旨意（原话）「把 qwen3.5-omni-plus 直转做成管线的第二种处理模式（双模式并存，上传时可选模式并显式指明模型，未来：模型可选，且 ASR+LLM（paraformer+deepseek）作为实时录音模式的入口）马上开干！！然后刷新服务效果给我看」

**结论：✅ 采纳，任务卡 T-S1.6 已派单**

- 探针实证（2026-08-28 大统领，同一段 5.5 分钟音频）：qwen3.5-omni-plus 直转 6.4s 出 17 轮清理稿——0 说话人错标、0 乱码（「诗雅乐」修成「是呀，那不然呢」）、0 幽灵轮次、他/她全篇正确；flash 版 7.4s/26 轮次之。plus 胜出。
- 决策：① 双模式并存，上传时可选并显式指明模型；② omni 直转为默认/推荐模式；③ ASR+LLM（paraformer+deepseek）保留，定位为未来实时录音模式入口；④ 模型可选留作未来扩展。
- 落实去向：T-S1.6（后端 omni provider + mode 字段 + 前端模式选择器），验收后重启服务演示给陛下。

---

## FB-009 · 2026-08-28 · 05号音频 omni 直转对比测试 + 二次清洗 + think 模式三连问

### 反馈（陛下旨意）
1. 用 `tests/audio/05_xhs_audio.mp3` 测 omni 直转，与 `05_xhs_good.md` 标准答案对比，找差异并分析原因
2. omni 处理完后能否再用 LLM 二次清洗判断？有何 skill 可提升 omni 结果质量？
3. 能否通过开启 think 模式提高准确度（角色识别/转写准确率/意图识别）？

### 实测结论（2026-08-28 大统领，05号 8分05秒音频，黄金稿 28 轮：来访者16/咨询师12）

**① omni 直转暴露系统性角色翻转（重大发现，生产已复现）**
- 线上同款 prompt：32 轮，逐轮对齐后角色仅 6 对 / 19 错（24% 正确率）——从第 2 轮起整体翻转
- 根因：本段是「角色反转」对话（来访者强势追问咨询师私生活，咨询师被动防御），omni 套用「提问者=咨询师」刻板先验，无视「雨生老师」称呼铁证
- 此前验证音频（01-04 + 陛下真实音频）全是常规问答模式，未暴露此缺陷；05 是测试集首个对抗样本
- 生产复现：会话41 走线上 omni 模式，同样翻转——确认是线上缺陷不是实验偶然
- 次要差异：人名听写不稳（雨生→雨辰/雨山，跨模型跨次漂移）、重复字未清（「是是」）、少量「嗯」幽灵轮

**② 增强 prompt 救回角色（已两次复跑验证）**
- 增强点：称呼语铁证锚定 + 显式警告存在角色反转 + 全篇一致性自查 + 重复字修正
- plus+增强 prompt：21 对 / 5 错（81%），两次复跑稳定；剩余 5 处错误集中在 #13/#17-20 内容歧义区
- ASR 管线（paraformer+deepseek）交叉验证同音频：20 对 / 5 错——声学分离对角色反转免疫，错在同一歧义区，可作仲裁参照

**③ think 模式：不可行 / 无收益（实测否决）**
- qwen3.5-omni-plus 不支持思考：`enable_thinking=true` 被静默忽略（0 reasoning tokens；阿里云文档确认 Qwen-Omni 系列仅 Qwen3-Omni-Flash 是混合思考模型）
- qwen3-omni-flash+thinking=true：思考真开了（470 reasoning tokens）但角色判定崩坏——61 轮全标咨询师、来访者识别为 0，且耗时 27.4s（3.3x）、token 7653（1.8x）
- 结论：思考只作用于感知之后的推理，救不了感知层（分离/听写）错误；不建议开

**④ 二次清洗：救不了角色翻转（结构性天花板，实测否决通用方案）**
- deepseek-v4-flash 通用二次清洗（4.5s）：无音频可听，照抄上游错误角色，0 挽救
- mimo-v2.5-pro 取证式复核（喂称呼语铁证提示，102s）：修对锚点轮但引入新错配，净收益为负
- 结论：声学信息在 omni 输出边界已丢光，纯文本二次清洗对角色/听错类错误无能为力；质量提升必须前置到转写 prompt（②已验证有效），备选路线为双模式（omni+ASR）分歧仲裁
- skill：`asr-transcript-cleanup` 已按本次教训更新（角色反转陷阱 + 称呼语锚定 + think 模式实测结论）

### 落实去向
- ✅ 全部实验存档 `/home/houmo/meng/omni_05_*`（7 组输出 + 对比脚本 + PDF 报告）
- ⏳ 待陛下拍板：`OMNI_TRANSCRIBE_PROMPT` 升级 v2（增强版，代码注释标「FB-008 拍板，照抄勿改写」，需新拍板）；可选升级项：双模式分歧仲裁
- 汇报时已按陛下要求回顾建议看板（本台账 FB-001~009 全量状态）

**追加拍板（2026-08-28）：陛下旨意（原话）「把增强版 prompt 升为线上 v2」**
- 结论：✅ 采纳，任务卡 T-S1.7 已派单（解除 omni.py 注释「FB-008 拍板，照抄勿改写」的锁定，prompt 升 v2）
- 实证依据：同音频角色正确率 24%→81%（两次复跑稳定），成本耗时不变（8.2s→9.1s）；剩余 5 处错误集中在内容歧义区（ASR 管线同样错，非 prompt 可解）

**追加反馈（2026-08-28）：陛下实测会话44 发现角色开头仍翻转**
- 现象：陛下经前端上传 05 号（会话44），v2 生效（输出含（笑）为证）但第2轮仍翻转，逐轮对齐 77%——prompt 软约束压不住采样随机性
- 追加探针（大统领实测，temperature=0 + v2 prompt 连跑 2 次）：run1 开头对 / run2 开头仍翻——**temp=0 也治不彻底**
- 结论：必须加确定性代码兜底。任务卡 T-S1.8 已派单：① 请求体加 `temperature: 0.0`（降低随机）；② 解析层「称呼语翻转校正」铁律——若「咨询师」轮内容以「某老师，」开头（称呼对方为老师的人不可能是老师本人），判定全篇角色翻转，代码自动对调全部轮次角色。确定性规则，不依赖模型自觉

**追加实测（2026-08-28，T-S1.8 验收）：全篇翻转铁律在生产误伤，实测证伪**
- 现象：T-S1.8 合并上线（e9b684c）后生产重跑 05 号（会话45），最终输出仅 6/26 = 23.1%——**比无兜底（77%）更差**
- 取证（sessions.raw_transcript 保存了校正前的模型原始输出）：
  - 会话45 原始输出只有**开头 2-8 轮**翻转，其余约 40 轮与黄金稿一致（原始稿直接评分 20/26 = 76.9%）
  - 铁律看到第2轮「咨询师：雨生老师，…」即触发**全篇对调**，把本来正确的后 40 轮全部打翻 → 23.1%
  - 会话44（陛下亲测）原始稿同为「仅开头翻转」模式；会话42 无翻转（铁律未触发，无损）
- 根因修正：v2 prompt 下 omni 的错误模式是**开头局部翻转**而非全篇翻转（全篇翻转是 v1 prompt 时代的模式）；「单点称呼证据 ⇒ 全篇翻转」假设证伪，全篇翻转属过度矫正
- 离线模拟正解：只把命中的那一轮「咨询师」就地改标「来访者」、其余不动 → 会话44/45 原始稿均可达 21/26 = 80.8%
- 结论：任务卡 T-S1.9 已派单——废除全篇翻转，改为**局部改标**（有界损伤、确定性、离线单测全覆盖）；temperature=0.0 保留（无害且略降随机）

---

## FB-010 · 2026-08-29 · 第二个上传卡住（分析完直接点下一个上传就卡）+ 左侧上传/分析记录看板

### 反馈（陛下原话）
> http://10.70.32.11:5199/ 这个服务好像坏了。另外我发现一个 bug：在上传并分析完一个后，再直接点上传下一个时候就会卡住，你修一下。另外开始增加上传和分析记录看板在页面左边，可以随时查询

### 诊断（大统领取证，2026-08-29）
1. **服务未坏**：mengpsy-backend(:8660)/frontend(:5199) 两个 systemd 单元自 8/28 起持续 active 无重启；GET /health=200；局域网入口 10.70.32.11:5199 返回 200；大统领真实连传 3 段音频（含 65s idle、4.5MB 大文件、5.1s keep-alive 竞态窗口 8 连发）全部成功。
2. **卡住铁证**：陛下会话 49（12:03）、50（12:08）均 done；12:08–12:25 后端访问日志**再无任何 POST /sessions**，同期 data/audio 无孤儿文件 → 第三发从未到达后端文件落盘层，死在浏览器/代理链路。
3. **根因（结构性，前端零防护）**：
   - ① `upload()` 的 POST fetch **无超时**：请求挂起则「处理中…」永久卡死，无错误提示、无法重试；
   - ② 轮询 GET fetch **无超时 + 无在途护栏**：任一请求挂起后，3 秒定时器继续发新请求，逐个挂起，耗尽浏览器每同源 6 连接上限 → 后续包括上传 POST 在内的所有请求永久排队 = 「点下一个上传卡住」；
   - ③ 触发条件为客户端网络瞬时抖动（WiFi 切换/锁屏/信号波动），服务端复现难以命中（大统领 4 轮 headless 复现含 65s idle 均未命中），但一旦命中前端无任何自愈手段。
4. 排除项：无 Service Worker；vite preview 代理链路实测健康；uvicorn keep-alive 5s 竞态 8 连发实测未触发。
5. 会话 42/43、45/46、47/48「44ms 双胞胎对」系大统领 T-S1.7/S1.9 浏览器验收脚本的双上传产物，非用户行为，与本案无关。

### 结论与落实去向
- ✅ 修复方案：上传/轮询加超时 + 在途护栏 + 连续失败退出，把「无限挂起」变成「可见可重试的错误」→ 任务卡 **T-S1.10 Part A**（已派单）。
- ✅ 新增左侧「上传与分析记录看板」（列表 + 点击回看详情）→ 任务卡 **T-S1.10 Part B**（陛下本次新增需求，同卡）。
- 服务入口当前可用，无需重启；后端若合入新代码由大统领重启 mengpsy-backend 并实测。

---

## FB-011 · 2026-08-29 · 记录标签/摘要可编辑 + 看板排序筛选/分组/删除/深色主题

### 反馈（陛下原话）
> 产生记录时，为每段记录生成标签/摘要，并允许编辑，可折叠可展开：标签用于分类与筛选（咨询/讲授/单人/多人…不限于）；摘要≤100字。上传与记录部分优化：1) 排序/筛选：上传时间戳+来源文件名+时长+记录字数；2) 折叠同一音频文件的记录，分组设置（增删改查），组编辑（组名/标签/备注）；3) 删除记录和组，删除前询问；4) 深色版主题。

### 结论与落实去向
- 拆两卡串行：**T-S1.11 后端**（数据模型+API：tags/brief/duration/word_count 富化、groups CRUD、PATCH/DELETE、record prompt v3 产 brief+tags、ffprobe 时长）→ 验收后 **T-S1.12 前端**（记录卡折叠/编辑、看板排序筛选/同文件折叠/分组管理/删除确认、深色主题）。均今日派单。
- 设计拍板（大统领按陛下旨意定）：标签/摘要存 sessions 表（看板直读，失败会话也可手工标）；旧记录摘要展示时截 record.summary 前 100 字兜底；删除=硬删（音频+segments+records+jobs 级联），确认框在前端做；排序筛选放前端（单用户数据量小），后端只富化字段。

---

## FB-012 · 2026-08-30 · 看板交互六项：分组上移/组双模式删除/批量选择导出/日历索引/右侧Tab重构/表格编辑视图

### 反馈（陛下原话）
> 1. 分组操作要放到上面来，否则每次都到最下面去找；2. 组信息可以二次编辑，增加组的编辑和删除按钮，提供「仅解散组」「删除组和包含的记录」两种删除模式；3. 记录看板对所有记录增加选择/全选/删除/导出；4. 增加日历可视化索引（点击日历看到当天的记录）；5. 优化右侧功能区布局，tab 切换：1）上传/转写区；2）记录详情区（要展示记录的所有信息，包括日期/时长/标签/摘要/转写记录等），点击查看记录时自动切换到记录详情区展示该条记录；6. 转写区顶部 tab 切换，气泡视图方便浏览，表格视图用于精确编辑。
> 总体要求：未来这是作为心理咨询师来访记录看板的，要以这个为目标去打造需求并实现。实现后对照 docs/prd/ 的 PRD 总结改进方向，优化 /home/houmo/meng/MengPanel.md 开发任务卡。

### 结论与落实去向
- 拆两卡串行：**T-S1.13 后端**（组删除双模式 dissolve/with_records、批量删除、导出端点、转写段编辑 PATCH）→ 验收后 **T-S1.14 前端**（六项交互重构）。
- 设计拍板（大统领按陛下旨意定）：
  - 「删除组和包含的记录」= 组成员会话逐条走既有硬删级联（segments/records/jobs/行+音频文件）；「仅解散组」= 现有行为（记录归未分组）。
  - 批量删除走新端点 `POST /api/sessions/bulk-delete`（一次确认、一次请求）；导出走新端点 `GET /api/sessions/export`（返回全量详情 JSON，前端下载）。
  - 表格视图精确编辑：新增 `PATCH /api/sessions/{id}/segments/{seq}` 改段落内容，保存后重建会话 cleaned_text（字数/记录输入随之更新）；说话人切换不在本卡范围（入 MengPanel 改进清单）。
  - 日历索引纯前端（按 started_at 本地日期聚合）；右侧两 Tab，点击看板记录自动切「记录详情」Tab；上传完成后自动切详情 Tab 展示新会话，保持现有上传→成稿动线不断。

---

## FB-013 · 2026-08-30 · 工作台数据看板（参考设计落地）+ 实时录音延后

### 反馈（陛下原话）
> 实时录音先不着急，先做出看板来。你能看懂这个参考设计吗？（附「朗庭心空间·工作台」参考截图：顶部 4 KPI 卡 + 中部来访档案/转写/AI督导 + 底部状态环形图/议题词云/情绪趋势/待办）

### 结论与落实去向
- 参考设计拆三层：①KPI 行（周环比口径）②来访档案闭环（依赖来访者代号体系 I-01，下一批）③数据看板（现有数据可做）。本批实现 ①+③。
- 拆两卡串行：**T-S1.15 后端**（`GET /api/dashboard/summary` 聚合统计）→ **T-S1.16 前端**（新增「工作台」页：4 KPI + 状态环形图 + 标签词云 + 时长趋势 + 待办提醒，纯 CSS/SVG 无新依赖，深色适配）。
- 设计拍板（大统领按陛下旨意定）：
  - 第 4 张 KPI 卡：参考稿为「进行中来访者」，当前无来访者体系，暂用「累计记录数」占位，I-01 落地后替换；
  - 环形图口径：会话状态分布（完成/处理中/失败）；词云口径：会话 tags 词频 Top20；趋势：近 14 天每日咨询分钟数；
  - 待办口径：待补摘要（brief 空）/ 待补标签（tags 空）/ 失败待重试（status=failed），点击跳对应记录；
  - 页面入口：页头加「记录看板 / 工作台」顶层切换，记录看板现状不动；
  - 实时录音（I-05）按陛下旨意延后，优先级降到来访者体系与看板之后。

---

## FB-014 · 2026-08-30 · 客户新需求（MengPRD.md）+ UI 风格拍板（atlantic.vc + idh_dashboard）

### 反馈（陛下原话）
> 1. UI风格和设计元素参考这个：https://atlantic.vc/，以及 /home/houmo/hermes_work/web/idh_dashboard_20260827.html，不要使用或渲染劣质的图标或图片
> 2. 收到客户新的需求了，你解读一下，并更新任务卡：/home/houmo/meng/MengPanel.md（需求原文 /home/houmo/meng/MengPRD.md）
> 3. 你的脚本又在 /home/houmo/meng 目录泛滥了，整理一下

### 解读与落实
- **需求解读入本条**：MengPRD.md 四节 = ①来访档案（姓名/性别/年龄/电话/紧急联系人×2/状态/开始时间/小节数/自由备注，全本地）②录音→转写→记录闭环（选来访者→新咨询→弹窗录音→最小化悬浮（仅计时+停止）→T/P 文字稿→法规无忧记录→咨询师补充（手动/语音）→个人私密笔记；且要求**灵活输入**：录音/文字稿/回忆叙述稿多途径）③界面：左列来访者名单→点击看其记录列表→「记录咨询」入口→录音最小化悬浮按钮可拖动 ④导出：记录/文字稿 × 当前/全部。
- **与现状对照**：来访者体系=I-01 扩展（加电话/紧急联系人等字段）；多途径输入中「文字稿/回忆叙述稿直接导入」为全新能力；语音补充=复用既有 ASR；个人私密笔记=新数据面；录音最小化可拖悬浮=新交互。
- **冲突提示**：实时录音在 FB-013 被陛下延后，客户需求③明确要录音——**陛下裁决（2026-08-30）：实时录音先不做**，先做批次 A 来访者档案；录音前置依赖（HTTPS 证书）一并延后。
- **UI 风格拍板（写死为设计基准，后续所有前端卡引用）**：
  1. 字体：`-apple-system, "SF Pro Display/Text"` 系统栈（idh 稿），中文跟随系统；
  2. 色板：克制低饱和——亮底 #F4F5F7/卡 #FFFFFF，暗底 #1C1F26/卡 #262B35，主色 #4A68D8（暗 #8A9BF0），状态色灰调化（ok #6FA87C / bad #C97A7A / warn #C99A5B / info #7A9CC9）；
  3. 形态：大圆角（卡 10-11px / 胶囊 99px）、轻阴影、无重边框；
  4. Atlantic 式版式：大字号 display 标题 + 斜体强调、编辑式留白、**禁止 emoji 图标与廉价图片**（现有 KPI 卡 📅⏱️🎯🗂️ 须移除，换排版式或单色 SVG 线条）；
  5. 图表延续纯 SVG（T-S1.16 基线），配上述色板。
- 脚本整理：根目录 9 个散落脚本归位 `scripts/verify/`（探查类入 `archive/`）、4 个派单日志归 `scripts/dispatch/`，根目录恢复清爽。

---

<!-- 新反馈从这里往上追加，格式参照 FB-001 -->
